version: 2.1

orbs:
  maven: circleci/maven@0.0.12
# Define the jobs we want to run for this project
jobs:
  build:
    - run:
        - mvn compile
workflows:
  build_and_test:
    jobs:
      - build